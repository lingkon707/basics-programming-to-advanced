let result = condition ? expressionIfTrue : expressionIfFalse;

// A shorthand for simple if-else statements, taking three operands: a condition,
// an expression to execute if true, and an expression to execute if false.
