for (expression1; expression2; expression3) {
    // Code block to be executed
}

// Parts explained:

// expression1 → Initialization (run once at the start)
// expression2 → Condition (loop runs while this is true)
// expression3 → Increment/Update (run after each iteration)