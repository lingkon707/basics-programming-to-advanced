 function calculateAndReturn(inputNumber) {
  // Add 100 to the input number
  let result = inputNumber + 100;

  // Return 50% of the result
  return 0.5 * result;
}

// Example usage:
let userInput = 120; // Replace with your desired input number
let finalResult = calculateAndReturn(userInput);
console.log(finalResult);
