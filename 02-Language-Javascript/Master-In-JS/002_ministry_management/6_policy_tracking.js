var policies = [
  {name: "Digital BD", status: "Running"},
  {name: "Green City", status: "Pending"}
];

for (var i = 0; i < policies.length; i++) {
  console.log(policies[i].name + " Status: " + policies[i].status);
}