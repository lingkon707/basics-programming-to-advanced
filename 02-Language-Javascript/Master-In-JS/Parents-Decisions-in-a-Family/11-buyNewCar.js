const oldCarCondition = "bad";
const savings = 400000;

if (oldCarCondition === "bad" && savings > 300000)
  console.log("🚗 Parents decided to buy a new car!");
else
  console.log("🚙 Continue using the old one for now.");
