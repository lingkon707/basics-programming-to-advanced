// IELTS overall band score calculation (average)
var listening = 7.5;
var reading = 6.5;
var writing = 6.0;
var speaking = 7.0;

var average = (listening + reading + writing + speaking) / 4;
console.log("Overall IELTS Band Score:", average.toFixed(1));