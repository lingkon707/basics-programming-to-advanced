const invoiceNumber = 101;
const clientName = "Lingkon";
const amountDue = 1500;

const invoiceMessage = `Invoice #${invoiceNumber} for ${clientName} is $${amountDue}`;
console.log(invoiceMessage);
// Invoice #101 for Lingkon is $1500
