// Example: Player performance rating
let runs = 45;
let playerRating = runs >= 50 ? "Star Player" : runs >= 20 ? "Support Player" : "Needs Improvement";
console.log(`Runs: ${runs}, Rating: ${playerRating}`);
