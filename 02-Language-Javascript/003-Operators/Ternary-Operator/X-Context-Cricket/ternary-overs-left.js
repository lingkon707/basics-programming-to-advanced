// Example: Overs left category
let oversLeft = 12;
let oversStatus = oversLeft >= 15 ? "Plenty of overs" : oversLeft >= 5 ? "Few overs left" : "Last over";
console.log(`Overs left: ${oversLeft}, Status: ${oversStatus}`);
