// Example: Captain decision
let tossWon = true;
let chooseToBat = false;
let decision = tossWon ? (chooseToBat ? "Bat first" : "Field first") : "Opposition decision";
console.log(`Toss Won: ${tossWon}, Decision: ${decision}`);
