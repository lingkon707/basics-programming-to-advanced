// Example: Team performance rating
let totalRuns = 250;
let totalWickets = 7;
let performance = totalRuns > 300 ? "Dominant" : totalWickets < 5 ? "Balanced" : "Average";
console.log(`Runs: ${totalRuns}, Wickets: ${totalWickets}, Performance: ${performance}`);
