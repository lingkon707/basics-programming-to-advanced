let friendA = true;  // available
let friendB = false; // not available

let marketGoer = friendA ? "Friend A goes to the market" : "Friend B goes to the market";
console.log(marketGoer); // Friend A goes to the market
