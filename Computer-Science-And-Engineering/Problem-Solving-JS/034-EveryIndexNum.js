let sentence = "I love you";

for (let i = 0; i < sentence.length; i++) {
  console.log(`Letter: ${sentence[i]}, Index: ${i}`);
}

/* explain : This code uses a for loop
to iterate through each letter in the
sentence and logs the letter along 
 with its index number. */
