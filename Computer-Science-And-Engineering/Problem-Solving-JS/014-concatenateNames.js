// concatenateNames.js

function concatenateNames(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

// Example usage
const first = "John";
const last = "Doe";

const fullName = concatenateNames(first, last);

console.log(fullName); // "John Doe"
