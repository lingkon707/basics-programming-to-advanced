let personGula: { name: string; age: number | string; active: boolean } = {
    name: "Lingkon",
    age: "25",   // could also be number
    active: true
};

console.log(personGula.name, personGula.age, personGula.active);
